{% extends 'base.html.twig' %}

{% block body %}
    <h1>Appartement</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appartement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ appartement.nom }}</td>
            </tr>
            <tr>
                <th>Nombrechambre</th>
                <td>{{ appartement.nombreChambre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <a class="btn btn-primary btn-sm" href="{{ path('appartement_index') }}">Retour à la liste</a>
        <a class="btn btn-primary btn-sm" href="{{ path('appartement_edit', { 'id': appartement.id }) }}">Modifier</a>
    </ul>
    <ul>
        {{ form_start(delete_form) }}
        <input class="btn btn-outline-danger btn-sm" type="submit" value="Supprimer">
        {{ form_end(delete_form) }}
    </ul>
{% endblock %}
